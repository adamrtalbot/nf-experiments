nextflow.enable.moduleBinaries = true
docker.enabled = true

wave {
    
    enabled = true
    freeze  = true
    build {
        repository      = "$REPOSITORY"
        cacheRepository = "$CACHE"
    }
}
